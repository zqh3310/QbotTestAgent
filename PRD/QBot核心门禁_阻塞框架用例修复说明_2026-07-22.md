# QBot 核心门禁阻塞、框架与用例修复说明

## 结论

- 修复范围：可信阻塞 3 条、框架问题 17 条、用例问题 3 条，共 23 条。
- 修复状态：23/23 已落实到 runner、Teams 宿主恢复、证据链或用例契约。
- 代码自测：根项目检查通过；Teams 自动化检查通过，54/54 测试通过。
- 用例簿自测：92 个 Case ID 唯一，首条 `SIT-INIT-002`、末条 `SIT-AUTH-005`；7 条受影响用例已改写；公式错误扫描 0；5 个工作表均已渲染复核。
- 边界：本次结论证明自动化框架和用例契约已完成自测，不把旧执行结果改写成产品通过；产品结论必须使用修复后的 runner 重新执行后产生。

## 可信阻塞修复（3/3）

| Case | 原因 | 修复 |
| --- | --- | --- |
| `SIT-SKILL-026` | 多技能所需技能不存在，错误归为环境阻塞 | 纳入真实 SkillHub Fixture 路由；Case 前安装 `qa-python-runtime`、`qa-node-runtime`，结束后清理；验证 2→1→2 chip/selectedSkills 闭环。Fixture 缺失改判框架错误。 |
| `SIT-TEAMS-DOC-001` | Teams Document 连接器无法稳定选中 | 使用权限感知 Teams Document MCP Fixture；按连接器 key 显式手动选择；分别核验 allowed/denied 文档的真实 `tools/call` 与不泄露正文。 |
| `SIT-KNOWLEDGE-001` | runner 未覆盖知识成果闭环 | 新增专用闭环：生成并确认正式成果，进入“知识-任务成果”，回读成果，再返回来源任务核验同一任务与成果。 |

## 框架问题修复（17/17）

| Case | 修复 |
| --- | --- |
| `SIT-INIT-004` | 改为读取 Teams 集成态 capabilities、输入区与发送门禁；不再依赖不存在的 `runtime-status-chip`。 |
| `SIT-INIT-025` | 改为验证宿主已准备运行环境、用户无需 CLI/PATH/user-data-dir；保留 ready 与明确失败态双分支。 |
| `SIT-TEAMS-NEW-002` | 重启后的运行中任务写入 transcript、reply-delta、reply_wait，且等待不少于 60 秒。 |
| `SIT-HOME-030` | Teams 全量 Fixture 中强制使用 renderer control adapter，使 feedback dry-run 路由可被真实捕获。 |
| `SIT-HOME-053` | 多轮 reply 证据按 label 累积；发送回执失败时仅在确认未提交、输入仍精确一致时允许第三次 Enter 回退。 |
| `SIT-HOME-058` | 识别为 10 分钟多轮 Case，逐轮发送并在每轮完成后立即落盘 transcript/reply-delta，避免第一轮超时导致第二轮丢失。 |
| `SIT-HITL-002` | 自定义 HITL 处理器补齐不少于 60 秒等待与 reply_wait 断言。 |
| `SIT-ISSUE-793` | 流式滚动专项处理器补齐回复证据、60 秒等待和 wait metadata。 |
| `SIT-ISSUE-800` | 三轮分别保存发送/回复证据和等待元数据；三轮无矛盾时形成确定性回归断言，不再误报阻塞。 |
| `SIT-EXPERT-013` | 删除确认只接受包含删除确认语义及明确破坏性按钮的 dialog，避免把专家详情弹层当确认框。 |
| `SIT-ART-021` | Teams fixture 重启后只收养同 executable、profile、loopback CDP 和 DEV 控制面的宿主；恢复后重新绑定 QWork page。 |
| `SIT-ART-013` | bridge 创建项目后 reload 同一 Teams WebView，等待工作台恢复，再从真实项目 UI 打开项目。 |
| `SIT-ART-014` | 与 ART-013 共用刷新和 UI 回读流程，消除项目已创建但页面列表未刷新的假失败。 |
| `SIT-MEM-001` | 新增跨四任务的记忆新增、读取、修改、删除闭环；多轮证据不再覆盖；纯函数门禁拒绝旧值残留。 |
| `SIT-SKILL-013` | 只定位 `QA Materialization Pending / qa-materialization-pending` 唯一 Fixture，缺失即框架错误，不再误点其他技能。 |
| `SIT-CONN-019` | 新增真实 Web 搜索处理器，要求官方 HTTPS 来源、日期和真实工具证据，并保存 turn context/diagnostics。 |
| `SIT-EXPERT-021` | 专家创建成功状态接受结构化 `currentExpert=expert-builder`，避免真实成功被旧 DOM 判定遗漏。 |

## 用例问题修复（3/3）

| Case | 修复 |
| --- | --- |
| `SIT-AUTH-003` | 把“关闭并重开同一 Teams/QWork live profile”定义为核心用户动作；要求重启前后身份、历史任务与日志对齐。 |
| `SIT-HOME-050` | 明确记录“输入唯一标题并查看结果”和“按 Esc 关闭”两个用户动作，并分别保存结果态和关闭态截图。 |
| `SIT-SKILL-032` | 用户结论只呈现失败依赖、主技能未安装和重试建议；HTTP、Fixture、内部错误仅写入脱敏原始证据 JSON。 |

## 自测记录

1. `npm run check`（仓库根目录）：通过，包含语法、调度器、框架不变量和 smoke。
2. `npm run check`（`teams360-automation`）：通过，54/54 测试通过。
3. 修订 JSON 契约校验：92 条、92 个唯一 ID、顺序正确，7 条修订用例均有步骤、成功判定和证据要求。
4. Excel 公式错误扫描：0 命中。
5. Excel 视觉复核：5 个工作表全量渲染；初始化/认证、侧栏搜索、技能、Teams 文档 4 个关键区域局部渲染通过。
